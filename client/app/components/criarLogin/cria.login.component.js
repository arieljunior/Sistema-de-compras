import template from './cria.login.html';
import controller from './cria.login.controller';

let criarLoginComponent = {
  restrict: 'E',
  bindings: {},
  template,
  controller
};

export default criarLoginComponent;
